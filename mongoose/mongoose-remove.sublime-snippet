<snippet>
	<content><![CDATA[
  ${1:Collection}.remove({ _id: res.locals.${2:collection}d })
    .then(() => {
      return res.json({ 'msg': '${1:Collection} Removed !' })
    })
    .catch(err => {
      return res.satus(400).json(err)
    })
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>mongoremove</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
